@model IEnumerable<Lab2_KASR_MAGZ.Models.Medicine>

@{
    ViewData["Title"] = "Index";

}

<h1>Purchase</h1>
<label><b>Only enter the data once</b></label>
<section>
    <form action="Purchase/ClientInformation" method="post">
        <label>Name</label>
        <input type="type" name="Name" value="" />
        <p>
        </p>
        <label>Nit</label>
        <input type="type" name="NIT" value="" />
        <p>
        </p>
        <label>Address</label>
        <input type="type" name="Direct" value="" />
        <button>Confirm</button>
    </form>
</section>
<p>
</p>
<section>
    <form action="Purchase/SearchMedicine" method="post">        
        <label><b>Search product</b></label>
        <input type="type" name="NameMS" value="" />
        <p>
        </p>
        <label><b>Quantity</b></label>
        <input type="type" name="quantity" value="" />
        <p>
        </p>
        <button>Add Products</button>
    </form>
</section>

<br />

<form asp-controller="Purchase" asp-action="DownloadBill">
    <input type="submit" value="Download Bill" />
</form>
<br />
<form asp-controller="Health" asp-action="Index">
    <input type="submit" value="Finalize" />
</form>
<br />

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductionHouse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Stock)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProductionHouse)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Stock)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
